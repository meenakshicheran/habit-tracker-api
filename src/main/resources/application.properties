spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/habitflow?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Meenakshi@07}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

server.port=${PORT:8080}

cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:3000}

jwt.secret=${JWT_SECRET:habitflow-secret-key-replace-in-production-must-be-32plus-chars}
jwt.expiration=${JWT_EXPIRATION:86400000}
